import styles from './pageHeader.module.css'

const Pageheader = () => {
    return (
        <header className={styles.header}>
            <h1>Titel</h1>
        </header>
    );
};

export default Pageheader;